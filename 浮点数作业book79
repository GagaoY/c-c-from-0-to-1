double CEIL(const double x);          // double向上取整数

double CEIL(const double x)         // double向上取整数
{
  long II=x;

  // 判断是否为整数
  if (x==II) return x;

  if (x>0)
  {
    II=x+1;
    return II;
  }
  else
  {
    II=x;
    return II;
  }
}
